import GameBoard from './gameboard';

export const playerBoard = new GameBoard();
export const computerBoard = new GameBoard();

export const playerTurn = (coords) => {
  const [x, y] = coords;
}

export const computerTurn = () => {

}
